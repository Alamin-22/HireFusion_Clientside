import { Helmet } from "react-helmet-async";

const Profile = () => {
    return (
        <div>
            <Helmet><title>Hire Fusion | Dashboard</title></Helmet>
            <h1>This is the Container of  Profile</h1>
        </div>
    );
};

export default Profile;